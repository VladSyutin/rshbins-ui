# Legacy Tokens

This folder is reserved for legacy-friendly token delivery artifacts.

The intended output is a small set of ready-to-include CSS custom properties that FOS developers can connect without running the full modern token pipeline inside their project.
