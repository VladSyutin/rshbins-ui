# Authentication Prototypes

This folder is reserved for authentication prototype pages and scenarios assembled from reusable templates in `src/components/templates/authentication/`.

Keep prototype files directly in this folder. Do not create a separate folder, docs page, or overview story for each screen. Prototype stories should render production-like responsive pages in fullscreen mode, so scenarios can be validated by resizing the browser or opening Storybook on real devices.
